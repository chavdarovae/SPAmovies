<div class="movie">
    <h2>Title: {{title}}</h2>
    <img
        src="{{imageUrl}}">
    <p>Available Tickets: {{tickets}}</p><button><a href="#/buyTickets/{{objectId}}">Buy Ticket</a></button>
    <button><a href="#/details/{{objectId}}">Details</a></button>
</div>